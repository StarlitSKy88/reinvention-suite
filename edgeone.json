{
  "$schema": "https://schemas.edgeone.app/edgeone.json",
  "name": "reinvention-suite",
  "build": {
    "command": "cd apps/web && pnpm install --frozen-lockfile=false && pnpm build",
    "outputDir": ".next",
    "framework": "nextjs"
  },
  "accelerateRegion": {
    "mainlandRegions": ["ap-guangzhou"],
    "overseasRegions": ["ap-singapore"]
  }
}
